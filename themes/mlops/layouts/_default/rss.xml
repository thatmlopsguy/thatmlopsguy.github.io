{{- $pctx := . -}}
{{- if .IsHome -}}
  {{- $pctx = .Site -}}
{{- end -}}
{{- $pages := slice -}}
{{- if .IsHome -}}
  {{- $pages = where .Site.RegularPages "Section" "in" .Site.Params.mainSections -}}
  {{- if not $pages -}}
    {{- $pages = where .Site.RegularPages "Section" "posts" -}}
  {{- end -}}
  {{- if not $pages -}}
    {{- $pages = .Site.RegularPages -}}
  {{- end -}}
{{- else -}}
  {{- $pages = .Pages -}}
{{- end -}}
{{- $limit := .Site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
  {{- $pages = $pages | first $limit -}}
{{- end -}}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>{{ if eq .Title .Site.Title }}{{ .Site.Title }}{{ else }}{{ with .Title }}{{ . }} | {{ end }}{{ .Site.Title }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>Recent content {{ if ne .Title .Site.Title }}{{ with .Title }}in {{ . }} {{ end }}{{ end }}on {{ .Site.Title }}</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>{{ site.Language.Locale | default site.LanguageCode | default "en-us" }}</language>
    {{ with .Site.Copyright }}<copyright>{{ . }}</copyright>{{ end }}
    {{ if not .Date.IsZero }}<lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    {{ with .OutputFormats.Get "rss" -}}
    {{ printf `<atom:link href="%s" rel="self" type="%s" />` .Permalink .MediaType | safeHTML }}
    {{ end -}}
    {{ range $pages }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      <guid isPermaLink="true">{{ .Permalink }}</guid>
      {{ $author := .Params.author | default site.Params.author.name | default site.Params.author }}
      {{ if $author }}
      <dc:creator xmlns:dc="http://purl.org/dc/elements/1.1/">{{ $author }}</dc:creator>
      {{ end }}
      {{ range .Params.tags }}
      <category>{{ . }}</category>
      {{ end }}
      <description>{{ .Summary | plainify | htmlEscape }}</description>
      {{ $cdata := printf "<content:encoded><![CDATA[%s]]></content:encoded>" .Content }}
      {{ $cdata | safeHTML }}
    </item>
    {{ end }}
  </channel>
</rss>
